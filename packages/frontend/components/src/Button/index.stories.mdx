import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import {Button, ButtonGroup} from '.'
import {Icon} from '../'

<Meta title="Components / Button" />


# Button
<Canvas>
    <Story name="default">
        <Button label='Button' />
    </Story>
    
    <Story name="primary">
        <Button primary label='Button' />
    </Story>
     <Story name="large">
        <Button label='Button' size="large" />
    </Story>
    <Story name="small">
        <Button label='Button' size="small" as="span" />
    </Story>
    <Story name="disabled">
        <Button label='Button' disabled  />
    </Story>
    <Story name="icons">
        <Button icon={<Icon.Delete />} size="small"  as="span" />
    </Story>
    <Story name="button group">
        <ButtonGroup>
            <Button icon={<Icon.Delete />} size="small"   as="span" />
            <Button icon={<Icon.Bold />} size="small"   as="span" />
            <Button icon={<Icon.Underline />} size="small"   as="span" />
        </ButtonGroup>
    </Story>
</Canvas>